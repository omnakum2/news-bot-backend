# 📰 Daily News Bot

A serverless bot that fetches latest news, summarizes it, and sends a daily email update.  
Built using Vercel, native fetch, and OpenAI for fast demo-ready execution.  
Trigger manually via `/api/demo` with optional `?email=` parameter.  
No database or AWS required for demo setup.